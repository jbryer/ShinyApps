require(shiny)

# Define UI Scatterplot Graph View
shinyUI(pageWithSidebar(
  
  # Application title
  headerPanel("Bivariate Scatterplot"),
  sidebarPanel(
# put shiny user interface code here

    ),
  mainPanel(
# put shiny code to display server output here
    
  )
))
